using UnityEngine;
using System.Collections.Generic;

public class Cat : MonoBehaviour
{

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // iterate through all cats in CatMiceGameManager.Instance.catList
        // if the cat is not already chasing a mouse, check if there is a mouse within maxSight and maxAngle
        // if there is a mouse within maxSight and maxAngle, start chasing the mouse
        
    }
}
